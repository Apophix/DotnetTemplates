# Default: normalize all text files to LF in the repo
* text=auto

# PowerShell scripts must use CRLF so Windows PowerShell 5.1 can parse them
*.ps1  text eol=crlf
*.psm1 text eol=crlf
*.psd1 text eol=crlf
